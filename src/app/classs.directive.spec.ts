import { ClasssDirective } from './classs.directive';

describe('ClasssDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasssDirective();
    expect(directive).toBeTruthy();
  });
});
